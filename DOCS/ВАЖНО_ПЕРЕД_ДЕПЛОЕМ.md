# ⚠️ ВАЖНО! Прочитайте перед деплоем

## 🎯 Ключевые моменты для успешного деплоя

### 1. ✅ Redis НЕ требуется!

**Важно:** Ваше приложение НЕ использует Redis напрямую в коде.

Все используют **PostgreSQL** через Solid* гемы:
- ✅ **Cache** → Solid Cache (PostgreSQL)
- ✅ **Jobs** → Solid Queue (PostgreSQL)
- ✅ **WebSocket** → Solid Cable (PostgreSQL)

**Вывод:** 
```
❌ НЕ создавайте Redis сервис в Coolify
❌ НЕ добавляйте переменную REDIS_URL
✅ Создайте только PostgreSQL 16
```

Это упростит деплой и уменьшит количество сервисов!

---

### 2. ⚡ Единственная база данных

**PostgreSQL 16** используется для:
- Основные данные приложения (users, projects, posts)
- Solid Cache (кеш)
- Solid Queue (фоновые задачи)
- Solid Cable (WebSocket)

**Миграции автоматически создадут все нужные таблицы!**

```bash
# При первом запуске выполнится:
rails db:prepare

# Это создаст:
# - Схему БД
# - Таблицы приложения
# - Таблицы solid_cache_entries
# - Таблицы solid_queue_*
# - Таблицы solid_cable_messages
```

---

### 3. 🔐 Все переменные = Runtime Only!

**КРИТИЧЕСКИ ВАЖНО:**

В Coolify при добавлении переменных окружения **ОБЯЗАТЕЛЬНО** помечайте как **"Runtime Only"**:

```
✅ Runtime Only:
   DATABASE_URL
   RAILS_MASTER_KEY
   SECRET_KEY_BASE
   TELEGRAM_BOT_TOKEN
   OPENROUTER_API_KEY

❌ НЕ Build Time!
```

**Почему?**
- Build Time = переменные попадут в Docker image
- Это опасно для секретов
- Может сломать сборку

---

### 4. 🔑 Два ключа безопасности

Вам нужно подготовить **ДВА** ключа:

#### A) RAILS_MASTER_KEY (уже есть):
```
995a2f3b6ea26667605e7b925ed0b195
```
Это из файла `config/master.key` - **используйте как есть**.

#### B) SECRET_KEY_BASE (нужно сгенерировать):
```bash
# Выполните на компьютере:
cd contentforce
bundle exec rails secret

# Скопируйте полученный результат
# Это будет длинная строка ~128 символов
```

**Оба ключа добавьте в Coolify как Runtime Only!**

---

### 5. 🌐 Домен нужен ДО деплоя

**Telegram и OpenRouter требуют реальный домен с HTTPS!**

```
❌ НЕ работает: http://IP:3000
✅ Работает: https://your-domain.com
```

**План действий:**

1. **ДО деплоя:** Настройте домен:
   - В Coolify → Application → Domains
   - Добавьте домен (например: `contentforce.yourdomain.com`)
   - Включите SSL (Let's Encrypt)

2. **В переменных окружения используйте домен:**
   ```bash
   TELEGRAM_ORIGIN_URL=https://contentforce.yourdomain.com
   OPENROUTER_SITE_URL=https://contentforce.yourdomain.com
   ```

3. **ПОСЛЕ деплоя:** Настройте Telegram webhook:
   ```bash
   # В Coolify Terminal:
   ./bin/rails console
   
   bot = Telegram::Bot::Client.new(ENV['TELEGRAM_BOT_TOKEN'])
   bot.set_webhook(url: "#{ENV['TELEGRAM_ORIGIN_URL']}/telegram/webhook")
   ```

---

### 6. 📦 Dockerfile уже готов

**НЕ изменяйте Dockerfile!**

Он уже оптимизирован для production:
- ✅ Multi-stage build
- ✅ Ruby 3.4.6
- ✅ Node.js 24.12.0
- ✅ Автоматическая миграция БД
- ✅ Непривилегированный пользователь
- ✅ Thruster для production

**Порт 3000** - уже настроен в Dockerfile (EXPOSE 3000).

---

### 7. 🗂️ Хранение файлов

**Текущая конфигурация:** Local storage в контейнере

**⚠️ ПРОБЛЕМА:** При перезапуске контейнера файлы теряются!

**Решения:**

#### Вариант A: AWS S3 (рекомендуется для production)
```bash
# Добавьте в Coolify:
AWS_ACCESS_KEY_ID=your_key
AWS_SECRET_ACCESS_KEY=your_secret
AWS_REGION=us-east-1
AWS_BUCKET=contentforce-uploads

# И измените в contentforce/config/environments/production.rb:
config.active_storage.service = :amazon  # вместо :local
```

#### Вариант B: Persistent Volume (проще)
```
1. Coolify → Application → Storage
2. Add Volume
3. Source: volume name (например: contentforce-storage)
4. Destination: /rails/storage
5. Сохранить и Redeploy
```

Если не используете загрузку файлов - можете оставить как есть.

---

### 8. 📊 Мониторинг первых запусков

**После деплоя обязательно проверьте логи!**

```
Coolify → Application → Logs:

✅ Должны видеть:
  [INFO] Starting server on 0.0.0.0:3000
  [INFO] Rails 8.1.2 application starting in production
  [INFO] Puma starting in cluster mode
  [INFO] * Listening on http://0.0.0.0:3000

❌ Не должно быть:
  [ERROR] Database connection failed
  [ERROR] Could not find RAILS_MASTER_KEY
  [ERROR] Redis connection refused
  [ERROR] Asset not found
```

**Первые 5 минут после деплоя:**
- Следите за логами в реальном времени
- Проверьте `/up` endpoint
- Попробуйте открыть главную страницу
- Если ошибки - смотрите раздел Troubleshooting

---

### 9. 🔄 Автоматический деплой (опционально)

**Настройте webhook для автоматического деплоя при push:**

1. **В Coolify:**
   - Application → Webhooks
   - Скопируйте Webhook URL

2. **В GitHub:**
   - Repository → Settings → Webhooks → Add webhook
   - Payload URL: вставьте URL из Coolify
   - Content type: `application/json`
   - Events: "Just the push event"
   - Save

Теперь каждый `git push origin main` будет автоматически деплоить!

---

### 10. 💾 Настройте бэкапы БД СРАЗУ!

**Не откладывайте это на потом!**

```
1. Coolify → PostgreSQL сервис → Backups
2. Enable automatic backups: ✅
3. Frequency: Daily (ежедневно)
4. Retention: 7 days (неделя)
5. Проверьте, что бэкап создается
```

**Первый бэкап создастся автоматически через 24 часа.**

Проверьте через день:
```
Coolify → PostgreSQL → Backups → View backups
```

---

## 📝 Минимальная конфигурация для старта

### Сервисы в Coolify (1):
```
✅ PostgreSQL 16
❌ Redis (НЕ нужен!)
```

### Обязательные переменные (10):
```
1. DATABASE_URL              (из PostgreSQL сервиса)
2. RAILS_ENV=production
3. RAILS_MASTER_KEY          (995a2f3b6ea26667605e7b925ed0b195)
4. SECRET_KEY_BASE           (сгенерировать: rails secret)
5. RAILS_SERVE_STATIC_FILES=true
6. RAILS_LOG_TO_STDOUT=true
7. TELEGRAM_BOT_TOKEN        (7608089982:AAGx...)
8. TELEGRAM_ORIGIN_URL       (https://ваш-домен.com)
9. OPENROUTER_API_KEY        (sk-or-v1-b3328247cb...)
10. OPENROUTER_API_URL       (https://openrouter.ai/api/v1)
```

### Рекомендуемые переменные (5):
```
11. RAILS_MAX_THREADS=5
12. WEB_CONCURRENCY=2
13. OPENROUTER_SITE_URL      (https://ваш-домен.com)
14. OPENROUTER_SITE_NAME=ContentForce
15. TELEGRAM_BOT_USERNAME    (ваш_бот_username)
```

---

## ⏱️ Время деплоя

```
Подготовка:           5 минут
Настройка Coolify:   15 минут
Деплой (первый раз):  10 минут
Настройка webhook:     3 минуты
─────────────────────────────
Итого:              ~30 минут
```

**Последующие деплои:** 2-5 минут (только Redeploy)

---

## 🚨 Частые ошибки

### Ошибка 1: "Database connection error"
```
Причина: DATABASE_URL неправильный или не Runtime Only
Решение: Проверьте URL и поставьте Runtime Only
```

### Ошибка 2: "Assets not found" (404 на CSS/JS)
```
Причина: RAILS_SERVE_STATIC_FILES не установлена
Решение: Добавьте RAILS_SERVE_STATIC_FILES=true
```

### Ошибка 3: "Secret key base is not set"
```
Причина: SECRET_KEY_BASE отсутствует
Решение: Сгенерируйте через rails secret и добавьте
```

### Ошибка 4: Telegram webhook не работает
```
Причина: URL не HTTPS или домен не настроен
Решение: Настройте домен с SSL в Coolify
```

### Ошибка 5: "Redis connection refused"
```
Причина: REDIS_URL указан, но Redis не нужен
Решение: УДАЛИТЕ переменную REDIS_URL (Redis не используется)
```

---

## ✅ Итоговый чеклист

### Перед деплоем:
```
[ ] Сгенерирован SECRET_KEY_BASE (rails secret)
[ ] Скопирован RAILS_MASTER_KEY (995a2f3b6ea26667605e7b925ed0b195)
[ ] Настроен домен в Coolify с SSL
[ ] PostgreSQL 16 создан и запущен
[ ] DATABASE_URL скопирован из PostgreSQL
[ ] Все 10 обязательных переменных добавлены
[ ] ВСЕ переменные помечены "Runtime Only"
[ ] Порт 3000 настроен в Application
```

### После деплоя:
```
[ ] Приложение открывается: https://домен.com
[ ] Health check работает: https://домен.com/up → "OK"
[ ] Логи без ошибок (Coolify → Logs)
[ ] Telegram webhook настроен (rails console)
[ ] Автоматические бэкапы БД включены
[ ] (Опционально) GitHub webhook настроен
```

---

## 📚 Документация

**Используйте в таком порядке:**

1. **ВАЖНО_ПЕРЕД_ДЕПЛОЕМ.md** ← Вы здесь
2. **БЫСТРЫЙ_СТАРТ.md** ← Пошаговые команды
3. **ИНСТРУКЦИЯ_ДЕПЛОЯ.md** ← Подробное описание
4. **ПРОВЕРКА_КОНФИГУРАЦИИ.md** ← Технические детали
5. **СХЕМА_ДЕПЛОЯ.md** ← Визуальные схемы

---

## 🎯 Главное

### ✅ Что НУЖНО:
- PostgreSQL 16
- 10 обязательных переменных (Runtime Only)
- Домен с HTTPS

### ❌ Что НЕ НУЖНО:
- Redis сервис
- Изменения в Dockerfile
- Дополнительные базы данных
- Сложная конфигурация

---

**Все готово! Следуйте БЫСТРЫЙ_СТАРТ.md и за 30 минут приложение будет работать! 🚀**

**Удачного деплоя! 🎉**
