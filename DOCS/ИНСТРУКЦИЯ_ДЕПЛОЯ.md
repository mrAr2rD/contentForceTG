# 📦 Инструкция по деплою ContentForce на Beget VPS через Coolify

## 🎯 Общая информация

Этот проект представляет собой Rails 8.1.2 приложение с:
- Ruby 3.4.6
- PostgreSQL 16
- Redis 7
- Node.js 24.12.0 (для сборки assets)
- Telegram Bot интеграция
- OpenRouter AI API

## 📋 Требования

### На вашем компьютере:
- Git
- Доступ к GitHub репозиторию

### На сервере Beget VPS:
- Ubuntu (уже установлена)
- Coolify (должен быть установлен)
- Минимум 2GB RAM
- 20GB свободного места

---

## 🚀 ШАГИ ДЕПЛОЯ

### Шаг 1: Проверка Dockerfile

Ваш Dockerfile уже готов к продакшену. Он использует:
- ✅ Multi-stage build для оптимизации размера образа
- ✅ Ruby 3.4.6
- ✅ Node.js 24.12.0
- ✅ Автоматическую миграцию БД через entrypoint
- ✅ Thruster для production сервера
- ✅ Порт 3000

**Никаких изменений в Dockerfile не требуется!**

---

### Шаг 2: Подготовка GitHub репозитория

1. **Убедитесь, что все изменения закоммичены:**
```bash
cd /Users/avdemkin/Documents/документы/CONTtg/contentforce
git status
git add .
git commit -m "Prepare for Coolify deployment"
git push origin main
```

2. **Проверьте, что эти файлы есть в репозитории:**
   - ✅ Dockerfile
   - ✅ docker-compose.yml
   - ✅ .dockerignore
   - ✅ config/master.key (НЕ должен быть в Git! Используем значение локально)

---

### Шаг 3: Настройка Coolify - Создание проекта

1. **Войдите в Coolify** на вашем Beget VPS
2. **Создайте новый проект:**
   - Нажмите "New Project"
   - Название: `ContentForce` (или любое другое)

3. **Добавьте приложение:**
   - Нажмите "Add Resource" → "Application"
   - Выберите "GitHub" как источник
   - Выберите ваш репозиторий
   - Branch: `main` (или тот, который используете)
   - Build Pack: `Dockerfile`

---

### Шаг 4: Настройка сервисов в Coolify

#### 4.1 Добавление PostgreSQL

1. В проекте нажмите "Add Resource" → "Database" → "PostgreSQL"
2. Выберите версию: **16** (важно!)
3. Название: `contentforce-db`
4. Запустите сервис
5. **Скопируйте DATABASE_URL** из настроек БД (будет выглядеть примерно так):
   ```
   postgresql://postgres:АВТОСГЕНЕРИРОВАННЫЙ_ПАРОЛЬ@postgres-xxxx:5432/postgres
   ```

#### 4.2 Добавление Redis

1. В проекте нажмите "Add Resource" → "Database" → "Redis"
2. Выберите версию: **7** (важно!)
3. Название: `contentforce-redis`
4. Запустите сервис
5. **Скопируйте REDIS_URL** из настроек Redis (будет выглядеть примерно так):
   ```
   redis://redis-xxxx:6379/0
   ```

---

### Шаг 5: Настройка переменных окружения приложения

Перейдите в настройки вашего приложения → "Environment Variables"

#### 5.1 Обязательные переменные (Runtime Only!)

**ВАЖНО:** Все переменные ниже должны быть помечены как **"Runtime Only"** (не "Build Time")!

```bash
# === База данных ===
DATABASE_URL=postgresql://postgres:ВАШ_ПАРОЛЬ@postgres-xxxx:5432/postgres
# ☝️ Вставьте URL из шага 4.1

# === Redis ===
REDIS_URL=redis://redis-xxxx:6379/0
# ☝️ Вставьте URL из шага 4.2

# === Rails Configuration ===
RAILS_ENV=production
RAILS_MAX_THREADS=5
WEB_CONCURRENCY=2
RAILS_SERVE_STATIC_FILES=true
RAILS_LOG_TO_STDOUT=true

# === Security Keys ===
RAILS_MASTER_KEY=995a2f3b6ea26667605e7b925ed0b195
# ☝️ Ваш реальный ключ из config/master.key

# Сгенерируйте новый SECRET_KEY_BASE:
SECRET_KEY_BASE=СГЕНЕРИРУЙТЕ_НОВЫЙ_КЛЮЧ
# ☝️ Смотрите раздел "Генерация ключей" ниже

# === Telegram Bot ===
TELEGRAM_BOT_TOKEN=7608089982:AAGx-Z4oG6qVIbqlva2Wwbt39nqNSZAi4YU
TELEGRAM_BOT_USERNAME=ваш_бот_username
TELEGRAM_ORIGIN_URL=https://ваш-домен.com
# ☝️ Замените на ваш реальный домен после деплоя

# === OpenRouter AI ===
OPENROUTER_API_KEY=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
OPENROUTER_API_URL=https://openrouter.ai/api/v1
OPENROUTER_SITE_URL=https://ваш-домен.com
OPENROUTER_SITE_NAME=ContentForce
```

#### 5.2 Опциональные переменные (если используете)

```bash
# === AWS S3 (для загрузки файлов) ===
# AWS_ACCESS_KEY_ID=ваш_ключ
# AWS_SECRET_ACCESS_KEY=ваш_секрет
# AWS_REGION=us-east-1
# AWS_BUCKET=contentforce-uploads

# === Sentry (для отслеживания ошибок) ===
# SENTRY_DSN=ваш_sentry_dsn

# === Robokassa (платежи) ===
# ROBOKASSA_MERCHANT_LOGIN=ваш_логин
# ROBOKASSA_PASSWORD_1=ваш_пароль_1
# ROBOKASSA_PASSWORD_2=ваш_пароль_2
```

---

### Шаг 6: Генерация ключей безопасности

На вашем компьютере выполните:

```bash
# Перейдите в папку проекта
cd /Users/avdemkin/Documents/документы/CONTtg/contentforce

# Сгенерируйте SECRET_KEY_BASE
bundle exec rails secret
```

Скопируйте полученный результат и вставьте в переменную `SECRET_KEY_BASE` в Coolify.

---

### Шаг 7: Настройка порта и домена

1. **Port Configuration:**
   - В настройках приложения найдите "Ports"
   - Установите: `3000` (Container Port)
   - Публичный порт: `80` или `443`

2. **Domain Configuration:**
   - В настройках приложения найдите "Domains"
   - Добавьте ваш домен (например: `contentforce.yourdomain.com`)
   - Включите SSL (Let's Encrypt) - рекомендуется

---

### Шаг 8: Запуск деплоя

1. **Сохраните все настройки** в Coolify
2. **Нажмите "Deploy"** или "Redeploy"
3. **Следите за логами** в реальном времени:
   - Coolify покажет процесс сборки
   - Дождитесь сообщения "Build successful"

#### Что происходит при деплое:

1. Coolify клонирует ваш репозиторий
2. Собирает Docker образ по Dockerfile:
   - Устанавливает Ruby gems
   - Устанавливает Node.js зависимости
   - Компилирует assets
   - Создает финальный образ
3. Запускает контейнер
4. Entrypoint автоматически выполняет `rails db:prepare` (создает/мигрирует БД)
5. Запускает Rails сервер через Thruster

**Время деплоя:** 5-10 минут (первый раз), 2-5 минут (последующие).

---

### Шаг 9: Проверка работоспособности

1. **Откройте ваш домен в браузере:**
   ```
   https://ваш-домен.com
   ```

2. **Проверьте health endpoint:**
   ```
   https://ваш-домен.com/up
   ```
   Должен вернуть "OK"

3. **Проверьте логи в Coolify:**
   - Перейдите в приложение → "Logs"
   - Убедитесь, что нет ошибок
   - Должны видеть: "Listening on http://0.0.0.0:3000"

---

## 🔧 Troubleshooting (Решение проблем)

### Проблема 1: "Database connection error"

**Решение:**
1. Проверьте, что PostgreSQL сервис запущен в Coolify
2. Убедитесь, что `DATABASE_URL` правильно скопирован
3. Проверьте, что `DATABASE_URL` помечен как "Runtime Only"

### Проблема 2: "Assets not found" или 404 на CSS/JS

**Решение:**
1. Убедитесь, что `RAILS_SERVE_STATIC_FILES=true` установлена
2. Проверьте логи сборки - assets должны компилироваться
3. Пересоберите приложение

### Проблема 3: "Redis connection refused"

**Решение:**
1. Проверьте, что Redis сервис запущен в Coolify
2. Убедитесь, что `REDIS_URL` правильно скопирован
3. Проверьте, что Redis и приложение в одной сети Coolify

### Проблема 4: "500 Internal Server Error"

**Решение:**
1. Проверьте логи в Coolify: "Logs" → "Application"
2. Убедитесь, что все обязательные переменные установлены
3. Проверьте, что `RAILS_MASTER_KEY` правильный
4. Выполните ручную миграцию (см. ниже)

### Проблема 5: Нужно выполнить команды вручную

**В Coolify:**
1. Перейдите в приложение → "Terminal"
2. Выполните нужные команды:

```bash
# Миграции вручную
./bin/rails db:migrate

# Создать админа
./bin/rails console
# В консоли: User.create!(email: 'admin@example.com', password: 'password')

# Проверить подключение к БД
./bin/rails db:version

# Очистить кеш
./bin/rails cache:clear
```

---

## 🔄 Обновление приложения (Редеплой)

Когда вы делаете изменения в коде:

1. **Закоммитьте и запушьте в GitHub:**
```bash
git add .
git commit -m "Your changes"
git push origin main
```

2. **В Coolify:**
   - Откройте приложение
   - Нажмите "Redeploy"
   - Или настройте Webhook для автоматического деплоя при push

### Настройка автоматического деплоя (опционально):

1. В GitHub репозитории: Settings → Webhooks → Add webhook
2. Payload URL: возьмите из Coolify (Applications → ваше приложение → "Webhooks")
3. Content type: `application/json`
4. Events: "Just the push event"
5. Теперь каждый push в main будет автоматически деплоить!

---

## 📊 Мониторинг

### Логи
- **Application logs:** Coolify → ваше приложение → "Logs"
- **Build logs:** Смотрите во время деплоя
- **Database logs:** PostgreSQL сервис → "Logs"

### Метрики
- **CPU/Memory:** Coolify показывает использование ресурсов
- **Disk space:** Следите за хранилищем

### Бэкапы
**Важно!** Настройте бэкапы базы данных:
1. Coolify → PostgreSQL сервис → "Backups"
2. Включите автоматические бэкапы
3. Установите расписание (например, ежедневно)

---

## ✅ Чек-лист перед деплоем

- [ ] Код закоммичен и запушен в GitHub
- [ ] PostgreSQL 16 сервис создан и запущен
- [ ] Redis 7 сервис создан и запущен
- [ ] Все обязательные переменные окружения установлены
- [ ] Все переменные помечены как "Runtime Only"
- [ ] SECRET_KEY_BASE сгенерирован
- [ ] Порт 3000 настроен
- [ ] Домен добавлен (опционально)
- [ ] SSL включен (опционально, но рекомендуется)

---

## 🎉 Готово!

После успешного деплоя ваше приложение будет доступно по адресу:
- `https://ваш-домен.com` (если настроен домен)
- Или по IP:PORT из Coolify

### Следующие шаги:

1. **Создайте первого пользователя/админа**
2. **Настройте Telegram Webhook:**
   ```bash
   # Через Rails console в Coolify Terminal:
   ./bin/rails console
   
   # В консоли:
   bot = Telegram::Bot::Client.new(ENV['TELEGRAM_BOT_TOKEN'])
   bot.set_webhook(url: "#{ENV['TELEGRAM_ORIGIN_URL']}/telegram/webhook")
   ```

3. **Протестируйте все функции**
4. **Настройте мониторинг (Sentry, если используете)**

---

## 📞 Полезные ссылки

- Coolify Documentation: https://coolify.io/docs
- Rails Guides: https://guides.rubyonrails.org
- Docker Documentation: https://docs.docker.com

---

## 💡 Советы

1. **Первый деплой занимает больше времени** (5-10 минут) - это нормально
2. **Используйте SSL** - Let's Encrypt бесплатный и легко настраивается в Coolify
3. **Настройте автоматические бэкапы БД** - это спасет вас в критической ситуации
4. **Мониторьте логи** первые несколько дней после деплоя
5. **Не коммитьте секреты** - они должны быть только в Coolify

---

**Готово! Ваше приложение должно успешно работать на Beget VPS через Coolify! 🚀**

Если возникнут проблемы - смотрите раздел Troubleshooting или проверяйте логи в Coolify.
