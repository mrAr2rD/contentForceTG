<div class="space-y-6">
  <div class="flex items-center justify-between">
    <div>
      <h2 class="text-2xl font-bold text-zinc-900 dark:text-zinc-50">Пригласительные ссылки</h2>
      <p class="mt-1 text-sm text-zinc-500 dark:text-zinc-400">
        Канал: <%= @telegram_bot.channel_name || @telegram_bot.channel_id %>
      </p>
    </div>
    <div class="flex items-center gap-3">
      <%= link_to subscriber_analytics_project_telegram_bot_path(@project, @telegram_bot), class: "inline-flex items-center gap-2 px-4 py-2 border border-zinc-300 dark:border-zinc-700 text-zinc-700 dark:text-zinc-300 rounded-lg text-sm font-medium hover:bg-zinc-50 dark:hover:bg-zinc-900 transition-colors" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M9 19v-6a2 2 0 00-2-2H5a2 2 0 00-2 2v6a2 2 0 002 2h2a2 2 0 002-2zm0 0V9a2 2 0 012-2h2a2 2 0 012 2v10m-6 0a2 2 0 002 2h2a2 2 0 002-2m0 0V5a2 2 0 012-2h2a2 2 0 012 2v14a2 2 0 01-2 2h-2a2 2 0 01-2-2z"/>
        </svg>
        Аналитика подписчиков
      <% end %>
      <%= link_to new_project_telegram_bot_invite_link_path(@project, @telegram_bot), class: "inline-flex items-center gap-2 px-4 py-2 bg-primary-600 text-white rounded-lg text-sm font-medium hover:bg-primary-700 transition-colors" do %>
        <svg class="w-4 h-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 4v16m8-8H4"/>
        </svg>
        Создать ссылку
      <% end %>
    </div>
  </div>

  <% if @invite_links.any? %>
    <div class="bg-white dark:bg-zinc-950 border border-zinc-200 dark:border-zinc-800 rounded-lg overflow-hidden">
      <table class="min-w-full divide-y divide-zinc-200 dark:divide-zinc-800">
        <thead class="bg-zinc-50 dark:bg-zinc-900">
          <tr>
            <th class="px-6 py-3 text-left text-xs font-medium text-zinc-500 dark:text-zinc-400 uppercase">Ссылка</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-zinc-500 dark:text-zinc-400 uppercase">Источник</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-zinc-500 dark:text-zinc-400 uppercase">Вступлений</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-zinc-500 dark:text-zinc-400 uppercase">Конверсия</th>
            <th class="px-6 py-3 text-left text-xs font-medium text-zinc-500 dark:text-zinc-400 uppercase">Статус</th>
            <th class="px-6 py-3 text-right text-xs font-medium text-zinc-500 dark:text-zinc-400 uppercase">Действия</th>
          </tr>
        </thead>
        <tbody class="divide-y divide-zinc-200 dark:divide-zinc-800">
          <% @invite_links.each do |link| %>
            <tr class="hover:bg-zinc-50 dark:hover:bg-zinc-900">
              <td class="px-6 py-4">
                <div>
                  <p class="text-sm font-medium text-zinc-900 dark:text-zinc-50"><%= link.name || 'Без названия' %></p>
                  <p class="text-xs text-zinc-500 dark:text-zinc-400 font-mono truncate max-w-xs"><%= link.invite_link %></p>
                </div>
              </td>
              <td class="px-6 py-4">
                <% if link.source.present? %>
                  <span class="inline-flex items-center px-2 py-1 rounded text-xs font-medium bg-blue-100 text-blue-700 dark:bg-blue-900/20 dark:text-blue-400">
                    <%= link.source %>
                  </span>
                <% else %>
                  <span class="text-xs text-zinc-400">—</span>
                <% end %>
              </td>
              <td class="px-6 py-4">
                <span class="text-sm font-semibold text-zinc-900 dark:text-zinc-50"><%= link.join_count %></span>
                <% if link.member_limit %>
                  <span class="text-xs text-zinc-500 dark:text-zinc-400">/ <%= link.member_limit %></span>
                <% end %>
              </td>
              <td class="px-6 py-4">
                <span class="text-sm <%= link.conversion_rate > 80 ? 'text-green-600 dark:text-green-400' : 'text-zinc-600 dark:text-zinc-400' %>">
                  <%= link.conversion_rate %>%
                </span>
              </td>
              <td class="px-6 py-4">
                <% if link.revoked? %>
                  <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-red-100 text-red-700 dark:bg-red-900/20 dark:text-red-400">
                    Отозвана
                  </span>
                <% elsif link.expired? %>
                  <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-zinc-100 text-zinc-600 dark:bg-zinc-800 dark:text-zinc-400">
                    Истекла
                  </span>
                <% elsif link.limit_reached? %>
                  <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-yellow-100 text-yellow-700 dark:bg-yellow-900/20 dark:text-yellow-400">
                    Лимит
                  </span>
                <% else %>
                  <span class="inline-flex items-center px-2 py-1 rounded-full text-xs font-medium bg-green-100 text-green-700 dark:bg-green-900/20 dark:text-green-400">
                    Активна
                  </span>
                <% end %>
              </td>
              <td class="px-6 py-4 text-right">
                <% if link.active? %>
                  <button onclick="navigator.clipboard.writeText('<%= link.invite_link %>')" class="text-sm text-primary-600 dark:text-primary-400 hover:underline mr-3">
                    Копировать
                  </button>
                  <%= link_to 'Отозвать', project_telegram_bot_invite_link_path(@project, @telegram_bot, link), method: :delete, data: { confirm: 'Отозвать ссылку? Это действие нельзя отменить.' }, class: "text-sm text-red-600 dark:text-red-400 hover:underline" %>
                <% end %>
              </td>
            </tr>
          <% end %>
        </tbody>
      </table>
    </div>
  <% else %>
    <div class="bg-white dark:bg-zinc-950 border border-zinc-200 dark:border-zinc-800 rounded-lg p-12 text-center">
      <svg class="w-12 h-12 mx-auto text-zinc-400 dark:text-zinc-500 mb-4" fill="none" stroke="currentColor" viewBox="0 0 24 24">
        <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M13.828 10.172a4 4 0 00-5.656 0l-4 4a4 4 0 105.656 5.656l1.102-1.101m-.758-4.899a4 4 0 005.656 0l4-4a4 4 0 00-5.656-5.656l-1.1 1.1"/>
      </svg>
      <p class="text-zinc-600 dark:text-zinc-400 mb-4">Пригласительные ссылки пока не созданы</p>
      <%= link_to 'Создать первую ссылку', new_project_telegram_bot_invite_link_path(@project, @telegram_bot), class: "inline-flex items-center gap-2 px-4 py-2 bg-primary-600 text-white rounded-lg text-sm font-medium hover:bg-primary-700 transition-colors" %>
    </div>
  <% end %>
</div>
