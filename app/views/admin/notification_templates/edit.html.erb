<div class="space-y-6">
  <div>
    <h2 class="text-2xl font-bold text-zinc-900 dark:text-zinc-50">Редактирование шаблона</h2>
    <p class="mt-1 text-sm text-zinc-500 dark:text-zinc-400"><%= @template.event_type.humanize %> / <%= @template.channel.titleize %></p>
  </div>

  <%= render 'form', template: @template %>
</div>
