<%= form_with model: [:dashboard, channel_site], class: 'space-y-8' do |form| %>
  <% if channel_site.errors.any? %>
    <div class="bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 rounded-xl p-4">
      <ul class="list-disc list-inside text-red-700 dark:text-red-300 text-sm">
        <% channel_site.errors.full_messages.each do |message| %>
          <li><%= message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <!-- Telegram Bot Selection -->
  <div class="bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl p-6">
    <h3 class="font-semibold mb-4">Канал</h3>

    <% if @telegram_bots.empty? %>
      <div class="bg-amber-50 dark:bg-amber-900/20 border border-amber-200 dark:border-amber-800 rounded-lg p-4 mb-4">
        <div class="flex items-start gap-3">
          <svg class="w-5 h-5 text-amber-500 flex-shrink-0 mt-0.5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
            <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
          </svg>
          <div>
            <p class="font-medium text-amber-800 dark:text-amber-200">Нет подходящих каналов</p>
            <p class="text-sm text-amber-700 dark:text-amber-300 mt-1">
              Для создания мини-сайта необходимо:
            </p>
            <ul class="text-sm text-amber-700 dark:text-amber-300 mt-2 list-disc list-inside space-y-1">
              <li>Подключить Telegram бота к проекту</li>
              <li>Добавить бота администратором канала</li>
              <li>Верифицировать бота в настройках проекта</li>
            </ul>
            <%= link_to 'Перейти к проектам', projects_path, class: 'inline-block mt-3 text-amber-800 dark:text-amber-200 underline hover:no-underline' %>
          </div>
        </div>
      </div>
    <% else %>
      <div class="space-y-4">
        <div>
          <%= form.label :telegram_bot_id, 'Telegram канал', class: 'block text-sm font-medium mb-2' %>
          <%= form.select :telegram_bot_id,
                          options_from_collection_for_select(@telegram_bots, :id, :display_name, channel_site.telegram_bot_id),
                          { include_blank: 'Выберите канал...' },
                          class: 'w-full px-4 py-2 border border-zinc-300 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900 focus:ring-2 focus:ring-emerald-500 focus:border-transparent' %>
          <p class="text-sm text-zinc-500 mt-1">
            Показаны только верифицированные боты-администраторы каналов.
          </p>
        </div>

        <%= form.hidden_field :project_id, value: @telegram_bots.first&.project_id %>
      </div>
    <% end %>
  </div>

  <!-- Domain Settings -->
  <div class="bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl p-6">
    <h3 class="font-semibold mb-4">Домен</h3>

    <div class="space-y-4">
      <div>
        <%= form.label :subdomain, 'Поддомен', class: 'block text-sm font-medium mb-2' %>
        <div class="flex items-center">
          <%= form.text_field :subdomain,
                              placeholder: 'mychannel',
                              class: 'flex-1 px-4 py-2 border border-zinc-300 dark:border-zinc-700 rounded-l-lg bg-white dark:bg-zinc-900 focus:ring-2 focus:ring-emerald-500 focus:border-transparent' %>
          <span class="px-4 py-2 bg-zinc-100 dark:bg-zinc-800 border border-l-0 border-zinc-300 dark:border-zinc-700 rounded-r-lg text-zinc-500">
            .contentforce.ru
          </span>
        </div>
        <p class="text-sm text-zinc-500 mt-1">Латинские буквы, цифры и дефисы. Минимум 3 символа.</p>
      </div>

      <div class="pt-4 border-t border-zinc-200 dark:border-zinc-800">
        <%= form.label :custom_domain, 'Свой домен (опционально)', class: 'block text-sm font-medium mb-2' %>
        <%= form.text_field :custom_domain,
                            placeholder: 'blog.example.com',
                            class: 'w-full px-4 py-2 border border-zinc-300 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900 focus:ring-2 focus:ring-emerald-500 focus:border-transparent' %>

        <!-- Инструкция по подключению домена -->
        <details class="mt-4 group">
          <summary class="flex items-center gap-2 cursor-pointer text-sm text-emerald-600 dark:text-emerald-400 hover:text-emerald-700 dark:hover:text-emerald-300">
            <svg class="w-4 h-4 transition-transform group-open:rotate-90" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
              <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
            </svg>
            Как подключить свой домен?
          </summary>

          <div class="mt-4 p-4 bg-zinc-50 dark:bg-zinc-800/50 rounded-lg border border-zinc-200 dark:border-zinc-700">
            <div class="space-y-5 text-sm">

              <!-- Вариант 1: Поддомен -->
              <div class="p-4 bg-emerald-50 dark:bg-emerald-900/20 border border-emerald-200 dark:border-emerald-800 rounded-lg">
                <div class="flex items-center gap-2 mb-3">
                  <span class="px-2 py-0.5 bg-emerald-500 text-white text-xs font-medium rounded">Рекомендуется</span>
                  <h4 class="font-semibold text-emerald-800 dark:text-emerald-200">Поддомен (blog.example.com)</h4>
                </div>
                <p class="text-emerald-700 dark:text-emerald-300 mb-3">
                  Самый простой способ — используйте поддомен вашего сайта:
                </p>

                <div class="space-y-3">
                  <div>
                    <p class="text-xs text-emerald-600 dark:text-emerald-400 mb-1 font-medium">1. Добавьте CNAME запись в DNS:</p>
                    <div class="bg-zinc-900 rounded-lg p-3 font-mono text-xs overflow-x-auto">
                      <table class="w-full text-zinc-300">
                        <thead>
                          <tr class="text-zinc-500 border-b border-zinc-700">
                            <th class="text-left pb-2 pr-4">Тип</th>
                            <th class="text-left pb-2 pr-4">Имя (Host)</th>
                            <th class="text-left pb-2">Значение (Value)</th>
                          </tr>
                        </thead>
                        <tbody>
                          <tr>
                            <td class="py-2 pr-4 text-emerald-400">CNAME</td>
                            <td class="py-2 pr-4 text-white">blog</td>
                            <td class="py-2 text-amber-400">contentforce.app</td>
                          </tr>
                        </tbody>
                      </table>
                    </div>
                  </div>

                  <div>
                    <p class="text-xs text-emerald-600 dark:text-emerald-400 mb-1 font-medium">2. Введите домен в поле выше:</p>
                    <code class="block px-3 py-2 bg-zinc-900 text-amber-400 rounded-lg font-mono text-xs">blog.example.com</code>
                  </div>
                </div>
              </div>

              <!-- Примеры для разных провайдеров -->
              <div>
                <h4 class="font-semibold text-zinc-900 dark:text-zinc-100 mb-3">Примеры настройки у популярных провайдеров</h4>

                <div class="space-y-3">
                  <!-- Cloudflare -->
                  <details class="group/cf">
                    <summary class="flex items-center gap-2 cursor-pointer text-zinc-700 dark:text-zinc-300 hover:text-zinc-900 dark:hover:text-zinc-100">
                      <svg class="w-4 h-4 transition-transform group-open/cf:rotate-90" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
                      </svg>
                      <span class="font-medium">Cloudflare</span>
                    </summary>
                    <div class="mt-2 ml-6 p-3 bg-zinc-100 dark:bg-zinc-800 rounded-lg text-xs space-y-2">
                      <p>1. Перейдите в <strong>DNS → Records → Add record</strong></p>
                      <p>2. Type: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">CNAME</code></p>
                      <p>3. Name: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">blog</code></p>
                      <p>4. Target: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">contentforce.app</code></p>
                      <p>5. Proxy status: <span class="text-amber-600 dark:text-amber-400">DNS only</span> (серое облако)</p>
                    </div>
                  </details>

                  <!-- REG.RU -->
                  <details class="group/reg">
                    <summary class="flex items-center gap-2 cursor-pointer text-zinc-700 dark:text-zinc-300 hover:text-zinc-900 dark:hover:text-zinc-100">
                      <svg class="w-4 h-4 transition-transform group-open/reg:rotate-90" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
                      </svg>
                      <span class="font-medium">REG.RU</span>
                    </summary>
                    <div class="mt-2 ml-6 p-3 bg-zinc-100 dark:bg-zinc-800 rounded-lg text-xs space-y-2">
                      <p>1. Личный кабинет → Домены → <strong>DNS-серверы и управление зоной</strong></p>
                      <p>2. Нажмите <strong>Добавить запись</strong></p>
                      <p>3. Тип записи: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">CNAME</code></p>
                      <p>4. Subdomain: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">blog</code></p>
                      <p>5. Canonical name: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">contentforce.app</code></p>
                    </div>
                  </details>

                  <!-- Beget -->
                  <details class="group/beget">
                    <summary class="flex items-center gap-2 cursor-pointer text-zinc-700 dark:text-zinc-300 hover:text-zinc-900 dark:hover:text-zinc-100">
                      <svg class="w-4 h-4 transition-transform group-open/beget:rotate-90" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
                      </svg>
                      <span class="font-medium">Beget</span>
                    </summary>
                    <div class="mt-2 ml-6 p-3 bg-zinc-100 dark:bg-zinc-800 rounded-lg text-xs space-y-2">
                      <p>1. Панель управления → <strong>DNS</strong></p>
                      <p>2. Выберите домен → <strong>Управление DNS-записями</strong></p>
                      <p>3. Добавить запись типа <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">CNAME</code></p>
                      <p>4. Поддомен: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">blog</code></p>
                      <p>5. Значение: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">contentforce.app.</code> (с точкой в конце)</p>
                    </div>
                  </details>

                  <!-- TimeWeb -->
                  <details class="group/tw">
                    <summary class="flex items-center gap-2 cursor-pointer text-zinc-700 dark:text-zinc-300 hover:text-zinc-900 dark:hover:text-zinc-100">
                      <svg class="w-4 h-4 transition-transform group-open/tw:rotate-90" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                        <path stroke-linecap="round" stroke-linejoin="round" d="M9 5l7 7-7 7"/>
                      </svg>
                      <span class="font-medium">TimeWeb</span>
                    </summary>
                    <div class="mt-2 ml-6 p-3 bg-zinc-100 dark:bg-zinc-800 rounded-lg text-xs space-y-2">
                      <p>1. Панель управления → <strong>Домены</strong> → выберите домен</p>
                      <p>2. Вкладка <strong>DNS</strong></p>
                      <p>3. Нажмите <strong>Добавить запись</strong></p>
                      <p>4. Тип: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">CNAME</code></p>
                      <p>5. Хост: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">blog</code>, Значение: <code class="px-1 bg-zinc-200 dark:bg-zinc-700 rounded">contentforce.app</code></p>
                    </div>
                  </details>
                </div>
              </div>

              <!-- TXT верификация -->
              <div class="pt-4 border-t border-zinc-200 dark:border-zinc-700">
                <h4 class="font-semibold text-zinc-900 dark:text-zinc-100 mb-2">Верификация домена (после создания сайта)</h4>
                <p class="text-zinc-600 dark:text-zinc-400 mb-3">
                  После создания мини-сайта вы получите уникальный токен. Добавьте TXT запись для подтверждения владения:
                </p>
                <div class="bg-zinc-900 rounded-lg p-3 font-mono text-xs overflow-x-auto">
                  <table class="w-full text-zinc-300">
                    <thead>
                      <tr class="text-zinc-500 border-b border-zinc-700">
                        <th class="text-left pb-2 pr-4">Тип</th>
                        <th class="text-left pb-2 pr-4">Имя</th>
                        <th class="text-left pb-2">Значение</th>
                      </tr>
                    </thead>
                    <tbody>
                      <tr>
                        <td class="py-2 pr-4 text-emerald-400">TXT</td>
                        <td class="py-2 pr-4 text-white">_contentforce.blog</td>
                        <td class="py-2 text-amber-400">contentforce-verify=ваш_токен</td>
                      </tr>
                    </tbody>
                  </table>
                </div>
                <p class="text-zinc-500 mt-2 text-xs">
                  DNS-записи обновляются от 5 минут до 24 часов. После этого нажмите "Проверить DNS" в настройках.
                </p>
              </div>

              <!-- Важно -->
              <div class="pt-3 border-t border-zinc-200 dark:border-zinc-700 space-y-2">
                <p class="text-zinc-500 flex items-start gap-2">
                  <svg class="w-4 h-4 flex-shrink-0 mt-0.5 text-amber-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M12 9v2m0 4h.01m-6.938 4h13.856c1.54 0 2.502-1.667 1.732-3L13.732 4c-.77-1.333-2.694-1.333-3.464 0L3.34 16c-.77 1.333.192 3 1.732 3z"/>
                  </svg>
                  <span><strong>Корневой домен (example.com):</strong> Не все провайдеры поддерживают CNAME для корневых доменов. Используйте поддомен (blog, news, channel) для надёжности.</span>
                </p>
                <p class="text-zinc-500 flex items-start gap-2">
                  <svg class="w-4 h-4 flex-shrink-0 mt-0.5 text-blue-500" fill="none" stroke="currentColor" viewBox="0 0 24 24" stroke-width="2">
                    <path stroke-linecap="round" stroke-linejoin="round" d="M13 16h-1v-4h-1m1-4h.01M21 12a9 9 0 11-18 0 9 9 0 0118 0z"/>
                  </svg>
                  <span><strong>SSL-сертификат:</strong> Будет выпущен автоматически после верификации домена (Let's Encrypt).</span>
                </p>
              </div>

            </div>
          </div>
        </details>
      </div>
    </div>
  </div>

  <!-- Site Settings -->
  <div class="bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl p-6">
    <h3 class="font-semibold mb-4">Настройки сайта</h3>

    <div class="space-y-4">
      <div>
        <%= form.label :site_title, 'Название сайта', class: 'block text-sm font-medium mb-2' %>
        <%= form.text_field :site_title,
                            placeholder: 'Название вашего канала',
                            class: 'w-full px-4 py-2 border border-zinc-300 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900 focus:ring-2 focus:ring-emerald-500 focus:border-transparent' %>
      </div>

      <div>
        <%= form.label :site_description, 'Описание', class: 'block text-sm font-medium mb-2' %>
        <%= form.text_area :site_description,
                           rows: 3,
                           placeholder: 'Краткое описание канала для посетителей',
                           class: 'w-full px-4 py-2 border border-zinc-300 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900 focus:ring-2 focus:ring-emerald-500 focus:border-transparent resize-none' %>
      </div>

      <div>
        <%= form.label :theme, 'Тема оформления', class: 'block text-sm font-medium mb-2' %>
        <%= form.select :theme,
                        [['Тёмная (по умолчанию)', 'default'], ['Светлая', 'light'], ['Минимальная', 'minimal']],
                        {},
                        class: 'w-full px-4 py-2 border border-zinc-300 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900 focus:ring-2 focus:ring-emerald-500 focus:border-transparent' %>
      </div>
    </div>
  </div>

  <!-- SEO Settings -->
  <div class="bg-white dark:bg-zinc-900 border border-zinc-200 dark:border-zinc-800 rounded-xl p-6">
    <h3 class="font-semibold mb-4">SEO</h3>

    <div class="space-y-4">
      <div>
        <%= form.label :meta_title, 'Meta Title', class: 'block text-sm font-medium mb-2' %>
        <%= form.text_field :meta_title,
                            maxlength: 70,
                            placeholder: 'Заголовок для поисковиков (до 70 символов)',
                            class: 'w-full px-4 py-2 border border-zinc-300 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900 focus:ring-2 focus:ring-emerald-500 focus:border-transparent' %>
      </div>

      <div>
        <%= form.label :meta_description, 'Meta Description', class: 'block text-sm font-medium mb-2' %>
        <%= form.text_area :meta_description,
                           rows: 2,
                           maxlength: 160,
                           placeholder: 'Описание для поисковиков (до 160 символов)',
                           class: 'w-full px-4 py-2 border border-zinc-300 dark:border-zinc-700 rounded-lg bg-white dark:bg-zinc-900 focus:ring-2 focus:ring-emerald-500 focus:border-transparent resize-none' %>
      </div>
    </div>
  </div>

  <!-- Submit -->
  <div class="flex items-center justify-end gap-4">
    <%= link_to 'Отмена', dashboard_channel_sites_path,
                class: 'px-4 py-2 text-zinc-600 dark:text-zinc-400 hover:text-zinc-900 dark:hover:text-zinc-100 transition-colors' %>
    <%= form.submit channel_site.persisted? ? 'Сохранить' : 'Создать',
                    class: 'px-6 py-2 bg-emerald-500 hover:bg-emerald-600 text-white rounded-lg transition-colors cursor-pointer' %>
  </div>
<% end %>
