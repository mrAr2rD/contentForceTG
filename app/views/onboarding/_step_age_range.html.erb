<div class="fade-in" id="onboarding_content">
  <!-- Question -->
  <div class="text-center mb-10">
    <p class="text-[#00D4AA] text-sm mono uppercase tracking-wider mb-3">Шаг 2 из 4</p>
    <h1 class="text-2xl md:text-3xl font-bold text-[#FAFAFA] mb-2">Сколько вам лет?</h1>
    <p class="text-[#A1A1AA]">Это поможет нам адаптировать интерфейс под вашу аудиторию</p>
  </div>

  <!-- Options grid -->
  <div class="grid grid-cols-2 md:grid-cols-5 gap-4">
    <% User::AGE_RANGES.each do |key, label| %>
      <%= button_to update_step_onboarding_path,
                    method: :post,
                    params: { step: "age_range", value: key },
                    class: "onboarding-option #{onboarding_data['age_range'] == key ? 'selected' : ''}",
                    data: { turbo_frame: "_top" } do %>
        <span class="icon"><%= age_range_icon(key) %></span>
        <span class="label"><%= label %></span>
      <% end %>
    <% end %>
  </div>
</div>
