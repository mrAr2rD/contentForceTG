<div class="max-w-4xl mx-auto px-4 py-8">
  <div class="flex justify-between items-center mb-6">
    <h1 class="text-2xl font-bold text-gray-900 dark:text-white">Telegram сессии</h1>
    <%= link_to "Добавить сессию", new_telegram_session_path, class: "inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors" %>
  </div>

  <div class="bg-white dark:bg-gray-800 rounded-xl shadow-sm border border-gray-200 dark:border-gray-700">
    <% if @telegram_sessions.any? %>
      <ul class="divide-y divide-gray-200 dark:divide-gray-700">
        <% @telegram_sessions.each do |session| %>
          <li class="p-4 flex items-center justify-between">
            <div class="flex items-center space-x-4">
              <div class="flex-shrink-0">
                <% if session.auth_auth_active? %>
                  <span class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-green-100 dark:bg-green-900">
                    <svg class="w-5 h-5 text-green-600 dark:text-green-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M5 13l4 4L19 7"/>
                    </svg>
                  </span>
                <% else %>
                  <span class="inline-flex items-center justify-center w-10 h-10 rounded-full bg-yellow-100 dark:bg-yellow-900">
                    <svg class="w-5 h-5 text-yellow-600 dark:text-yellow-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                      <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M12 8v4l3 3m6-3a9 9 0 11-18 0 9 9 0 0118 0z"/>
                    </svg>
                  </span>
                <% end %>
              </div>
              <div>
                <p class="text-sm font-medium text-gray-900 dark:text-white">
                  <%= session.masked_phone %>
                </p>
                <p class="text-sm text-gray-500 dark:text-gray-400">
                  <% if session.auth_auth_active? %>
                    Активна
                  <% elsif session.auth_pending_code? %>
                    Ожидает код
                  <% elsif session.auth_pending_2fa? %>
                    Ожидает 2FA
                  <% else %>
                    Истекла
                  <% end %>
                  &middot; <%= time_ago_in_words(session.created_at) %> назад
                </p>
              </div>
            </div>
            <div class="flex items-center space-x-2">
              <%= button_to telegram_session_path(session), method: :delete, data: { turbo_confirm: "Удалить эту сессию?" }, class: "p-2 text-gray-400 hover:text-red-600 transition-colors" do %>
                <svg class="w-5 h-5" fill="none" stroke="currentColor" viewBox="0 0 24 24">
                  <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M19 7l-.867 12.142A2 2 0 0116.138 21H7.862a2 2 0 01-1.995-1.858L5 7m5 4v6m4-6v6m1-10V4a1 1 0 00-1-1h-4a1 1 0 00-1 1v3M4 7h16"/>
                </svg>
              <% end %>
            </div>
          </li>
        <% end %>
      </ul>
    <% else %>
      <div class="p-8 text-center">
        <svg class="mx-auto h-12 w-12 text-gray-400" fill="none" stroke="currentColor" viewBox="0 0 24 24">
          <path stroke-linecap="round" stroke-linejoin="round" stroke-width="2" d="M8 12h.01M12 12h.01M16 12h.01M21 12c0 4.418-4.03 8-9 8a9.863 9.863 0 01-4.255-.949L3 20l1.395-3.72C3.512 15.042 3 13.574 3 12c0-4.418 4.03-8 9-8s9 3.582 9 8z"/>
        </svg>
        <h3 class="mt-2 text-sm font-medium text-gray-900 dark:text-white">Нет сессий</h3>
        <p class="mt-1 text-sm text-gray-500 dark:text-gray-400">
          Авторизуйте Telegram аккаунт для импорта истории каналов
        </p>
        <div class="mt-6">
          <%= link_to "Добавить сессию", new_telegram_session_path, class: "inline-flex items-center px-4 py-2 bg-blue-600 text-white rounded-lg hover:bg-blue-700 transition-colors" %>
        </div>
      </div>
    <% end %>
  </div>
</div>
