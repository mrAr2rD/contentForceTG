<!DOCTYPE html>
<html lang="ru" data-controller="theme">
  <head>
    <!-- Prevent FOUC - Load theme before rendering -->
    <script>
      (function() {
        const theme = localStorage.getItem('theme') || 'light';
        if (theme === 'dark') {
          document.documentElement.classList.add('dark');
        }
      })();
    </script>

    <title><%= content_for(:title) || seo_title %></title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <meta name="apple-mobile-web-app-capable" content="yes">
    <meta name="mobile-web-app-capable" content="yes">
    <% if (desc = seo_description).present? %>
      <meta name="description" content="<%= desc %>">
    <% end %>
    <%= seo_robots_tag %>
    <link rel="canonical" href="<%= seo_canonical_url %>">

    <!-- OpenGraph -->
    <meta property="og:type" content="website">
    <meta property="og:title" content="<%= seo_og_title %>">
    <% if seo_og_description.present? %>
      <meta property="og:description" content="<%= seo_og_description %>">
    <% end %>
    <meta property="og:url" content="<%= seo_canonical_url %>">
    <meta property="og:site_name" content="<%= SiteConfiguration.current.effective_site_name %>">
    <% if seo_og_image.present? %>
      <meta property="og:image" content="<%= seo_og_image %>">
    <% end %>

    <!-- Twitter Card -->
    <meta name="twitter:card" content="summary_large_image">
    <meta name="twitter:title" content="<%= seo_og_title %>">
    <% if seo_og_description.present? %>
      <meta name="twitter:description" content="<%= seo_og_description %>">
    <% end %>
    <% if seo_og_image.present? %>
      <meta name="twitter:image" content="<%= seo_og_image %>">
    <% end %>

    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= yield :head %>

    <%# Enable PWA manifest for installable apps (make sure to enable in config/routes.rb too!) %>
    <%#= tag.link rel: "manifest", href: pwa_manifest_path(format: :json) %>

    <link rel="icon" href="/icon.png" type="image/png">
    <link rel="icon" href="/icon.svg" type="image/svg+xml">
    <link rel="apple-touch-icon" href="/icon.png">

    <%# Includes all stylesheet files in app/assets/stylesheets %>
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_include_tag "application", "data-turbo-track": "reload", type: "module" %>

    <%= render_analytics_scripts %>
  </head>

  <body class="bg-white dark:bg-zinc-950 text-zinc-900 dark:text-zinc-50">
    <% if notice.present? %>
      <div class="fixed top-20 right-4 z-[100] bg-green-50 dark:bg-green-900/20 border border-green-200 dark:border-green-800 text-green-800 dark:text-green-100 px-6 py-4 rounded-lg shadow-lg max-w-md">
        <p class="font-medium"><%= notice %></p>
      </div>
    <% end %>

    <% if alert.present? %>
      <div class="fixed top-20 right-4 z-[100] bg-red-50 dark:bg-red-900/20 border border-red-200 dark:border-red-800 text-red-800 dark:text-red-100 px-6 py-4 rounded-lg shadow-lg max-w-md">
        <p class="font-medium"><%= alert %></p>
      </div>
    <% end %>

    <%= yield %>

    <%= render "shared/sponsor_banner" %>
  </body>
</html>
