# 🎯 Схема деплоя ContentForce на Coolify

## 📊 Архитектура приложения

```
┌─────────────────────────────────────────────────────────────┐
│                     Beget VPS Ubuntu                         │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │                      Coolify                            │ │
│  │                                                         │ │
│  │  ┌──────────────┐  ┌──────────────┐  ┌─────────────┐  │ │
│  │  │ PostgreSQL   │  │    Redis     │  │ Application │  │ │
│  │  │     16       │  │      7       │  │  (Docker)   │  │ │
│  │  │              │  │ (опционально)│  │             │  │ │
│  │  │  Port: 5432  │  │  Port: 6379  │  │  Port: 3000 │  │ │
│  │  └──────┬───────┘  └──────┬───────┘  └──────┬──────┘  │ │
│  │         │                 │                  │         │ │
│  │         └─────────────────┴──────────────────┘         │ │
│  │                    Internal Network                    │ │
│  └────────────────────────────────────────────────────────┘ │
│                                                              │
│  ┌────────────────────────────────────────────────────────┐ │
│  │              Nginx Reverse Proxy (Coolify)             │ │
│  │              SSL Termination (Let's Encrypt)           │ │
│  └────────────────────┬───────────────────────────────────┘ │
└───────────────────────┼─────────────────────────────────────┘
                        │
                        ▼
                  Port 80/443
                        │
                        ▼
                   Internet
                        │
         ┌──────────────┼──────────────┐
         ▼              ▼              ▼
    Browser      Telegram API    OpenRouter API
```

---

## 🔄 Процесс деплоя (пошагово)

```
1. GitHub Repository
   │
   │ git push
   │
   ▼
2. Coolify
   │
   │ git clone
   │
   ▼
3. Docker Build
   │
   ├─ Install Ruby 3.4.6
   ├─ Install Node.js 24.12.0
   ├─ Bundle install (gems)
   ├─ Yarn install (npm packages)
   ├─ Precompile assets
   └─ Build final image
   │
   ▼
4. Docker Run
   │
   ├─ Load environment variables (Runtime Only)
   ├─ Run entrypoint: rails db:prepare
   └─ Start: thruster + rails server
   │
   ▼
5. Running Application
   │
   ├─ Listening on 0.0.0.0:3000
   ├─ Connected to PostgreSQL
   ├─ Connected to Redis (if needed)
   └─ Ready to accept requests
   │
   ▼
6. Nginx Proxy (Coolify)
   │
   ├─ SSL termination (HTTPS)
   ├─ Proxy to container:3000
   └─ Serve to Internet
```

---

## 🗄️ Структура базы данных

```
PostgreSQL Database
│
├── Application Tables
│   ├── users
│   ├── projects
│   ├── posts
│   └── telegram_bots
│
├── Solid Cache Tables
│   └── solid_cache_entries
│
├── Solid Queue Tables
│   ├── solid_queue_jobs
│   ├── solid_queue_ready_executions
│   ├── solid_queue_claimed_executions
│   ├── solid_queue_blocked_executions
│   ├── solid_queue_failed_executions
│   └── solid_queue_recurring_tasks
│
└── Solid Cable Tables
    └── solid_cable_messages
```

---

## 🔐 Переменные окружения (минимальный набор)

```
┌─────────────────────────────────────────────────────────┐
│              ОБЯЗАТЕЛЬНЫЕ (Runtime Only)                 │
├─────────────────────────────────────────────────────────┤
│ DATABASE_URL          → PostgreSQL connection            │
│ REDIS_URL             → Redis connection (optional)      │
│ RAILS_ENV             → production                       │
│ RAILS_MASTER_KEY      → 995a2f3b6ea26667605e7b925ed0b195│
│ SECRET_KEY_BASE       → <generate with rails secret>    │
│ TELEGRAM_BOT_TOKEN    → 7608089982:AAGx...              │
│ TELEGRAM_ORIGIN_URL   → https://your-domain.com         │
│ OPENROUTER_API_KEY    → sk-or-v1-b3328247cb...          │
├─────────────────────────────────────────────────────────┤
│              РЕКОМЕНДУЕМЫЕ                               │
├─────────────────────────────────────────────────────────┤
│ RAILS_MAX_THREADS     → 5                               │
│ WEB_CONCURRENCY       → 2                               │
│ RAILS_SERVE_STATIC    → true                            │
│ RAILS_LOG_TO_STDOUT   → true                            │
│ OPENROUTER_API_URL    → https://openrouter.ai/api/v1    │
│ OPENROUTER_SITE_URL   → https://your-domain.com         │
│ OPENROUTER_SITE_NAME  → ContentForce                    │
│ TELEGRAM_BOT_USERNAME → your_bot_username               │
└─────────────────────────────────────────────────────────┘
```

---

## 📦 Что включено в Docker образ

```
Docker Image (~1.5GB)
│
├── Base Layer
│   ├── Ruby 3.4.6
│   ├── PostgreSQL client
│   ├── libvips (image processing)
│   └── jemalloc (memory optimization)
│
├── Application Layer
│   ├── Ruby gems (from Gemfile.lock)
│   ├── Precompiled bootsnap
│   ├── Compiled assets (CSS, JS)
│   └── Application code
│
└── Runtime Configuration
    ├── User: rails (UID 1000)
    ├── Workdir: /rails
    ├── Entrypoint: bin/docker-entrypoint
    └── Command: thrust + rails server
```

---

## ⚡ Что происходит при запуске контейнера

```
Container Start
│
▼
1. Load Environment Variables
   ├─ DATABASE_URL
   ├─ RAILS_MASTER_KEY
   └─ Other secrets
│
▼
2. Run Entrypoint (bin/docker-entrypoint)
   ├─ Enable jemalloc
   └─ Run: rails db:prepare
      ├─ Create database (if not exists)
      ├─ Load schema
      └─ Run migrations
│
▼
3. Start Thruster
   ├─ HTTP/2 support
   ├─ Static file serving
   ├─ Gzip compression
   └─ Proxy to Rails
│
▼
4. Start Rails Server (Puma)
   ├─ Load application
   ├─ Connect to PostgreSQL
   ├─ Connect to Redis (if needed)
   ├─ Start workers
   └─ Listen on 0.0.0.0:3000
│
▼
5. Ready to accept requests! ✅
```

---

## 🔄 Жизненный цикл запроса

```
User Request (HTTPS)
│
▼
Nginx (Coolify)
├─ SSL termination
└─ Proxy to container:3000
│
▼
Thruster (Port 3000)
├─ Serve static files (if exists)
│  └─ /assets/*.css, *.js, images
├─ Compress response (gzip)
└─ Proxy to Puma
│
▼
Puma Web Server
├─ Receive request
└─ Pass to Rails
│
▼
Rails Application
├─ Router (config/routes.rb)
├─ Controller (app/controllers/)
├─ Model (app/models/)
│  └─ PostgreSQL query
├─ View (app/views/)
└─ Response
│
▼
Thruster
├─ Add caching headers
└─ Compress response
│
▼
Nginx (Coolify)
└─ Send to user
│
▼
User sees response ✅
```

---

## 🛠️ Интеграции

```
ContentForce Application
│
├── Telegram Bot API
│   ├─ Webhook: /telegram/webhook
│   ├─ Bot commands
│   └─ Message handling
│
├── OpenRouter AI API
│   ├─ Content generation
│   ├─ AI chat
│   └─ Text processing
│
├── PostgreSQL Database
│   ├─ User data
│   ├─ Content storage
│   └─ Cache/Queue/Cable
│
└── Redis (optional)
    ├─ Session storage
    └─ Custom caching
```

---

## 🎯 Минимальная конфигурация для старта

### В Coolify нужно создать:

```
1. Project
   └─ "ContentForce"

2. Services (2)
   ├─ PostgreSQL 16
   │  └─ contentforce-db
   └─ Redis 7 (optional)
      └─ contentforce-redis

3. Application (1)
   ├─ Source: GitHub
   ├─ Branch: main
   ├─ Build: Dockerfile
   ├─ Port: 3000
   └─ Environment Variables (10 обязательных)
```

### Переменные окружения (минимум):

```
Обязательные (10):
1. DATABASE_URL
2. RAILS_ENV=production
3. RAILS_MASTER_KEY
4. SECRET_KEY_BASE
5. RAILS_SERVE_STATIC_FILES=true
6. RAILS_LOG_TO_STDOUT=true
7. TELEGRAM_BOT_TOKEN
8. TELEGRAM_ORIGIN_URL
9. OPENROUTER_API_KEY
10. OPENROUTER_API_URL
```

---

## ⏱️ Таймлайн деплоя

```
┌─────────────────────────────────────────────────────────┐
│ Время      │ Действие                                   │
├────────────┼───────────────────────────────────────────┤
│ 0:00-0:05  │ Подготовка (генерация ключей)             │
│ 0:05-0:15  │ Настройка Coolify (сервисы + приложение)  │
│ 0:15-0:17  │ Git clone                                  │
│ 0:17-0:25  │ Docker build (установка зависимостей)     │
│ 0:25-0:28  │ Asset compilation                          │
│ 0:28-0:29  │ Final image creation                       │
│ 0:29-0:30  │ Container start + db:prepare               │
│ 0:30       │ ✅ Application ready!                      │
└─────────────────────────────────────────────────────────┘

Первый деплой: ~30 минут
Последующие: ~5 минут (кеширование слоев Docker)
```

---

## 🚦 Проверка работоспособности

```
Проверка           │ Команда/URL                   │ Ожидаемый результат
───────────────────┼───────────────────────────────┼─────────────────────
Health check       │ https://domain.com/up         │ "OK" (200)
Homepage           │ https://domain.com/           │ Главная страница
Database           │ rails db:version              │ Версия миграций
Console            │ rails console                 │ Rails console >
Telegram webhook   │ bot.get_webhook_info          │ URL + certificate
Logs               │ Coolify → Logs                │ No errors
PostgreSQL         │ psql -U postgres              │ Подключение OK
Redis (optional)   │ redis-cli ping                │ PONG
```

---

## 💡 Оптимизация (после деплоя)

### Performance:
```
1. Увеличить workers:
   WEB_CONCURRENCY=4
   RAILS_MAX_THREADS=10

2. Настроить job workers:
   JOB_CONCURRENCY=2

3. Увеличить connection pool:
   DATABASE_POOL=10
```

### Storage:
```
1. Настроить S3 для файлов:
   - Создать S3 bucket
   - Добавить AWS credentials
   - Изменить active_storage.service = :amazon

2. Или настроить Persistent Volume в Coolify:
   - Storage → Add Volume
   - Mount: /rails/storage
```

### Monitoring:
```
1. Подключить Sentry:
   SENTRY_DSN=your_dsn

2. Настроить логирование:
   RAILS_LOG_LEVEL=warn (для production)

3. Включить автоматические бэкапы БД:
   Coolify → PostgreSQL → Backups → Enable
```

---

## 🎉 Готово к деплою!

**Используйте:**
- **БЫСТРЫЙ_СТАРТ.md** - для пошагового деплоя
- **ИНСТРУКЦИЯ_ДЕПЛОЯ.md** - для подробной информации
- **ПРОВЕРКА_КОНФИГУРАЦИИ.md** - для проверки конфигурации

**Минимальное время деплоя: 30 минут** 🚀
